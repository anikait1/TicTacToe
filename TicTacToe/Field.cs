using System;
using System.Collections.Generic;
using System.Text;

namespace TicTacToe
{
    /// <summary>
    /// This enum represents the different values which a cell on the gameboard can hold.
    /// </summary>
    public enum Field
    {
        #region Enum Members
        Empty = ' ',
        X = 'X',
        O = 'O'
        #endregion
    }
}
